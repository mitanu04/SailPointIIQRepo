# Build output — regenerated every build, never commit
build/
dependency-check-reports/
dependency-check-data/
Program FilesApache Software FoundationTomcat 9.0webappsidentityiq/

# Product binaries — re-downloadable from Compass (remove these 3 lines if you want them tracked)
base/ga/
base/patch/
base/efix/
base/ap/
exportedObjects/**

# Secrets — environment credentials must never be committed
build.properties
servers.properties
*.target.properties
*.iiq.properties
*.target.secret.properties
dev.iiq.cfg
dev.iiq.dat
*.iiq.cfg
*.iiq.dat


# Exported jar of custom Java (rebuilt from src/)
**/identityiqCustomizations.*.jar

# OS / IDE noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log